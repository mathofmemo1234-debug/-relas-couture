# دليل استضافة متجر ريلاس للأزياء على GitHub Pages وربطه بـ Firebase

أهلاً بكِ في الدليل الشامل لتشغيل ونشر موقع **"ريلاس لفساتين السهرة والزفاف" (Relas Couture)** مجاناً على الإنترنت وربطه بقاعدة بيانات سحابية وتخصيص الواتساب.

---

## 🌟 محتويات المشروع وميزاته

1. **المتجر الرئيسي (`index.html`)**: تصميم كوتور فاخر بنمط روزا كلارا، يعرض تشكيلات فساتين الزفاف والسهرة والملكة مع محاكي أقساط تابي وتمارا، ومعاينة سريعة لكل فستان.
2. **استوديو أخذ القياسات التفاعلي (`measurements.html`)**: مانيكان ذكي بـ 13 نقطة قياس تفاعلية، قوائم منسدلة للأرقام، توليد فوري لرسالة الواتساب، وإمكانية طباعة كرت القياسات الكلاسيكي.
3. **لوحة التحكم الشاملة (`admin.html`)**: لإدارة الفساتين، مراجعة وتحديث حالات طلبات القياسات، تغيير رقم الواتساب في ثوانٍ، وتعديل مفاتيح Firebase.

---

## 🚀 أولاً: خطوات الاستضافة المجانية على GitHub Pages (في 3 دقائق)

تتيح لكِ منصة GitHub استضافة الموقع مجاناً مدى الحياة وبسرعة فائقة وبشهادة أمان SSL مجانية (رابط آمن يبدأ بـ `https://`).

### الطريقة الأولى: عبر واجهة موقع GitHub (الأسهل بدون أوامر)

1. **إنشاء حساب ومستودع جديد**:
   - افتحي موقع [GitHub.com](https://github.com) وسجلي الدخول (أو أنشئي حساباً مجانياً).
   - اضغطي على زر **New Repository** (أو علامة `+` أعلى اليمين).
   - سمي المستودع: `relas-couture` (أو أي اسم تفضلينه).
   - اختاري أن يكون المستودع **Public** (عام) ثم اضغطي **Create repository**.

2. **رفع ملفات المشروع**:
   - في صفحة المستودع الجديد، اضغطي على خيار **uploading an existing file** (رفع ملفات موجودة).
   - اسحبي كافة ملفات ومجلدات المشروع الموجودة في جهازك:
     - `index.html`
     - `measurements.html`
     - `admin.html`
     - مجلد `css/` (ويحتوي على `style.css`)
     - مجلد `js/` (ويحتوي على `app.js`, `mannequin.js`, `firebase-service.js`, `admin.js`, `data-initial.js`)
     - `README.md`
   - اضغطي على الزر الأخضر في الأسفل: **Commit changes**.

3. **تفعيل الاستضافة بضغطة زر (GitHub Pages)**:
   - من أعلى صفحة المستودع على GitHub، اضغطي على تبويب **Settings** (الإعدادات).
   - من القائمة الجانبية على اليسار، اضغطي على **Pages**.
   - في قسم **Build and deployment**:
     - عند خيار **Branch**: اختاري `main` (أو `master`).
     - عند المجلد: اتركي الخيار كما هو `/ (root)`.
     - اضغطي على زر **Save** (حفظ).

4. **الوصول لموقعكِ الحي**:
   - بعد دقيقة أو دقيقتين، سيظهر لكِ في نفس الصفحة رابط موقعكِ المباشر بالأعلى:
   - `https://your-username.github.io/relas-couture/`
   - أصبح موقعكِ متاحاً لجميع العميلات حول العالم! ✨

---

## 🔥 ثانياً: خطوات إنشاء وربط قاعدة بيانات Firebase Firestore (مجاناً)

الموقع مصمم بتقنية ذكية **(Dual Storage Engine)**؛ فهو يعمل فوراً بالتخزين المحلي الآمن، وعند رغبتكِ في مزامنة البيانات سحابياً بين مختلف الأجهزة والهواتف:

1. **إنشاء مشروع Firebase**:
   - افتحي [Firebase Console](https://console.firebase.google.com).
   - سجلي الدخول بحساب Gmail.
   - اضغطي على **Add project** (إضافة مشروع) وسميه مثلاً `relas-couture-db` ثم اضغطي **Continue**.

2. **تفعيل قاعدة بيانات Firestore Database**:
   - من القائمة الجانبية اضغطي على **Build** ثم **Firestore Database**.
   - اضغطي على **Create database**.
   - اختاري موقع الخادم (مثل: `europe-west3` أو الأقرب للسعودية).
   - في خطوة الأمان (Security rules) اختاري **Start in test mode** ثم اضغطي **Enable**.

3. **الحصول على مفاتيح الربط (Firebase Config)**:
   - اضغطي على أيقونة الترس ⚙️ أعلى القائمة الجانبية بجانب Project Overview ثم اختاري **Project settings**.
   - انزلي لأسفل الصفحة واضغطي على أيقونة الويب `</>` (Add app).
   - سمي التطبيق `relas-web` ثم اضغطي **Register app**.
   - ستظهر لكِ أكواد تحتوي على كائن `firebaseConfig` مثل:
     ```javascript
     const firebaseConfig = {
       apiKey: "AIzaSy...",
       authDomain: "relas-couture.firebaseapp.com",
       projectId: "relas-couture",
       storageBucket: "relas-couture.appspot.com",
       messagingSenderId: "123456789",
       appId: "1:123456:web:abcd"
     };
     ```

4. **تفعيل الربط في لوحة تحكم ريلاس (بدون لمس الكود)**:
   - افتحي صفحة لوحة التحكم لموقعك: `admin.html`.
   - أدخلي كلمة المرور (الافتراضية: `123456`).
   - انتقلي إلى تبويب **⚙️ إعدادات المتجر والواتساب**.
   - انزلي إلى قسم **ربط قاعدة بيانات Firebase Firestore**.
   - الصقي قيم: `API Key` و `Project ID` و `App ID`.
   - اضغطي على **حفظ وتفعيل Firebase 🔥**.
   - سيظهر لكِ فوراً المؤشر الأخضر بالأعلى: `قاعدة بيانات Firebase Firestore متصلة ومفعّلة سحابياً 🟢`.

---

## 💬 ثالثاً: تغيير وإدارة رقم الواتساب الافتراضي

الموقع مجهز بنظام تحكم فوري برقم الواتساب:

1. افتحي لوحة التحكم `admin.html`.
2. ادخلي تبويب **⚙️ إعدادات المتجر والواتساب**.
3. في حقل **رقم الواتساب الافتراضي المعتمد**:
   - اكتبي رقمكِ الجديد مع مفتاح الدولة (بدون علامة + وبدون مسافات).
   - مثال: `966551234567` أو `966509876543`.
4. اضغطي على زر **تجربة الرقم 📲** للتأكد من فتح المحادثة على رقمكِ الصحيح.
5. اضغطي على **حفظ كافة الإعدادات والواتساب 💾**.
6. يتم تحديث كافة الأزرار في الموقع الرئيسي واستوديو القياسات وكروت الفساتين فورياً وبشكل دائم!

---

## 📐 رابعاً: استوديو القياسات وبطاقة المشغل الكلاسيكية

- **النقاط الـ 13 المعتمدة**:
  1. الطول الكلي
  2. طول الرقبة
  3. عرض الكتفين
  4. المسافة بين الصدرين
  5. محيط الصدر
  6. محيط تحت الصدر
  7. محيط الخصر
  8. طول قصة الصدر
  9. محيط الورك
  10. ارتفاع الورك
  11. طول الذراع
  12. عرض الزندة
  13. دوران المعصم
- عند ملء الاستمارة والضغط على إرسال، يتم:
  1. حفظ الطلب في قاعدة البيانات مع رقم تسلسلي (مثل `ORD-2026-1042`).
  2. فتح محادثة واتساب موجهة لمصممة ريلاس برسالة مجهزة ومنسقة بكل القياسات.
  3. إمكانية طباعة "كرت المشغل" الكلاسيكي الأصفر المطابق لبطاقة ريلاس للأزياء.

---

## 🔒 معلومات تسجيل الدخول الافتراضية للوحة التحكم
- **الرابط**: `admin.html`
- **كلمة المرور / الرمز الافتراضي**: `123456`
- *(يمكنكِ تغيير كلمة المرور في أي وقت من داخل لوحة التحكم في تبويب الإعدادات)*.
